# Errors and Recovery

| Date | Trigger | Impact | Containment | Recovery | Validation evidence |
|---|---|---|---|---|---|
| 2026-08-27 | DOCX visual renderer could not find LibreOffice/soffice on the local runtime PATH | DOCX page-image QA could not be completed in this environment | Rebuilt the DOCX and checked OOXML structure plus content parity; PDF was rendered and visually inspected separately | Install/provide LibreOffice, then run the packaged DOCX renderer before hardware handoff | DOCX generation exit 0; PDF 13 pages rendered; structural checks passed; visual DOCX QA remains open |
| 2026-08-29 | `pyorbbecsdk2==2.1.2` imported, but Gemini 335 SDK open failed with `usbEnumerator openUsbDevice failed!` / Access denied (status 113) | No stream started; zero timestamp samples; P0 calibration/time-sync and rosbag collection remain pending | Adapter wrote `failed_incomplete` manifest and preserved the SDK log; no permission bypass or motor control attempted | Add approved udev rule or `video`-group access, start a new login/session, repeat the attended preflight, then run the 30-s stationary check | `artifacts/evidence/p0_capture_preflight_20260829T024616Z/capture_manifest.json` (SHA-256 `49660e8dcf412865b525936f61ace16d5c25ec99efda88c490e8d2997dcc02c7`); `Log/OrbbecSDK.log.txt`; `id -nG` lacks `video`; `/dev/video*` are `root:video` |
| 2026-08-30 | Gemini 335 SDK logs report output FrameSet queue drops and depth payload-size mismatch drops during short SDK captures | Individual frames may be omitted; the observed 27.50 Hz depth/IR rate cannot be treated as a 30 Hz acceptance result, and no loss-rate acceptance has been measured | Evidence remains `captured_unassessed`/`not_measured`; no interpolation, synthesis, or rate-pass claim was made | Diagnose USB bandwidth/CPU callback backlog and depth transport behavior; repeat a bounded capture with frame-loss instrumentation before setting any rate, loss, or synchronization threshold | `Log/OrbbecSDK.log.txt` lines 1696-1733; `artifacts/evidence/p0_depth_quality_capture_20260830T072043Z/`; `assessment_cli_20260830T083000Z/assessment.json` |
| 2026-08-30 | ROS 2 executable and official Orbbec ROS driver remain unavailable, despite user-space `rosbags`/`mcap` being installable | No official ROS topics, TF, camera-info, QoS, or `ros2 bag play` replay can be claimed | Added a clearly labeled normalized-sample MCAP diagnostic container; source rows are preserved exactly and no missing values are synthesized | Install authorized ROS 2 Humble, rosbag2-storage-mcap, tf2, and matching Orbbec driver before the required ROS bag route | `artifacts/evidence/p0_stationary_loss_instrumented_20260830T081936Z/userspace_mcap_cli_v2/package_manifest.json`; `docs/p0/INDOOR_ROSBAG_PROTOCOL.md` |
| 2026-08-30 | Adapter selected the gyro-named sample-rate enum for accel even when a distinct `OBAccelSampleRate` symbol exists | Future SDKs with distinct accel enum classes could reject or misconfigure explicit accel profiles | Preferred `OBAccelSampleRate` with a legacy fallback; added a distinct-enum regression test | Re-run hardware profile verification after any SDK upgrade | `conda run -n sparseworld pytest -q` -> 61 passed; `conda run -n sparseworld pip check` -> No broken requirements found |
| 2026-08-31 | Field calibration/time-offset evidence remains absent despite available factory snapshot and SDK capture data | No checkerboard residual, camera-IMU/base-camera transform, or reproducible absolute clock offset can be accepted | Added fail-closed evidence-integrity checker and operator template; missing raw evidence is `not_measured`, not inferred from factory values or clocks | Provide checkerboard/fixture, rigid installation measurement, and shared-clock or hardware-triggered timing data; then run `assess-calibration` on raw evidence | `src/sparseworld_p0/calibration.py`; `config/p0_calibration_evidence.example.json`; `docs/p0/CALIBRATION_AND_TIME_SYNC.md` |
| 2026-09-02 | ROS 2 system installation initially required an interactive sudo password in the non-interactive session | Official driver preflight and rosbag collection were temporarily unavailable | User-authorized installation was completed; retain the reviewed base installer as fallback and record the superseding clean build below | Keep ROS C++ builds outside Conda and re-run the pinned overlay checks after host changes | Historical `sudo -n true` exit 1; superseded by `/home/ubuntu/ros2_ws/log-systempy4/latest_build/logger_all.log` |
| 2026-09-02 | Official driver build first lacked `cv_bridge`, then had Conda OpenSSL/cURL ABI contamination, then lacked `diagnostic_updater` | The first build artifacts were not runnable | Installed ROS/system dependencies and rebuilt with system Python in a clean environment; final overlay is `install-systempy4` | Keep ROS C++ builds outside Conda; source `/opt/ros/humble/setup.bash` and `/home/ubuntu/ros2_ws/install-systempy4/setup.bash` | `/home/ubuntu/ros2_ws/log-systempy4/latest_build/logger_all.log`; final summary: 3 packages finished |
| 2026-09-02 | First driver run lacked image transport plugins; unthrottled `ros2 topic hz` subscribers induced `ROS_PUBLISH` backpressure | Optional compressed topics were absent; probe-run drops cannot be attributed to camera/USB transport | Installed `ros-humble-image-transport-plugins`; clean 8-second rerun has no plugin errors; retained the probe effect as a measurement-method risk | Use raw topics and bounded subscribers for data-quality checks; separate subscriber overload from camera loss | `artifacts/evidence/p0_ros2_stationary_20260902T111500Z/plugin_recheck.log`; `orbbec_sdk_driver.log` |
