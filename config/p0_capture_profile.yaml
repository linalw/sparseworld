schema_version: p0/v1
device:
  manufacturer: Orbbec
  model: Gemini 335
  usb_vid_pid: "2bc5:0800"
  serial: CP0F4630001M
  v4l_by_id: "current USB device exposes video0-video5"
  firmware: pending_measurement
frames:
  tree: [map, odom, base_link, camera_link, camera_optical_frame]
  imu_parent: base_link
  imu_frame: imu_link
  intrinsics: pending_measurement
  extrinsics: pending_measurement
streams:
  rgb: {resolution: 1280x720, nominal_rate: 30, format: MJPG}
  depth: {resolution: 848x480, nominal_rate: 30, format: Y16}
  left: {resolution: 848x480, nominal_rate: 30, format: Y8}
  right: {resolution: 848x480, nominal_rate: 30, format: Y8}
  imu: {resolution: pending_measurement, nominal_rate: pending_measurement}
map:
  origin: local_initialization_without_external_datum
  units: SI
  timestamp_standard: UTC ISO-8601
quality_gates:
  stationary_calibration: pending_measurement
  hand_carried_supervised_route: pending_measurement
time_gates:
  device_host_offset: pending_measurement
  timestamp_pairing_policy: pending_measurement
diagnostics:
  window_seconds: 30
  storage: bounded_local_dense
  dense_buffer:
    local_origin_frame: camera_link
    timestamp_field: UTC ISO-8601
    ttl_seconds: 30
topology:
  requires_realtime_clearance_check: true
scope:
  motor_control: false
  claims: "P0 capture observability only; not P1 localization, navigation, or safety performance."
